import { Layout } from "@/components/layout/Layout";
import { HeroSection } from "@/components/blocks/hero-section-2";

const Demo = () => {
  return (
    <Layout>
      <HeroSection />
    </Layout>
  );
};

export default Demo;
